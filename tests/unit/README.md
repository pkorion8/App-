# Unit tests

Reserved. No unit test runner is wired up yet — add one (Vitest is the
natural fit given the TS-first stack) when the first package has real
logic worth unit-testing beyond what typecheck/lint already catch.
