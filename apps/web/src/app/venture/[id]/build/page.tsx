import type { Metadata } from "next";
import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { isSupabaseConfigured } from "@venture-sandbox/integrations";
import type { RecommendedStack, BacklogItem, CostEstimate } from "@venture-sandbox/build";
import { Badge, BarList, Card, StatTile, type BadgeStatus } from "@venture-sandbox/ui";
import { SupabaseSetupNotice } from "@/components/SupabaseSetupNotice";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { VentureModeSection } from "../VentureModeSection";
import { GenerateButton } from "./GenerateButton";

export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Build" };

const CATEGORY_LABEL: Record<string, string> = { setup: "Setup", core: "Core", auth: "Login", payments: "Payments", polish: "Polish", launch: "Launch" };
const CATEGORY_BADGE_STATUS: Record<string, BadgeStatus> = { setup: "neutral", core: "primary", auth: "neutral", payments: "warning", polish: "neutral", launch: "success" };

export default async function BuildPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const configured = isSupabaseConfigured({ url: process.env.NEXT_PUBLIC_SUPABASE_URL, anonKey: process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY });
  if (!configured) return <SupabaseSetupNotice />;
  const supabase = await createSupabaseServerClient();
  const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/sign-in");
  const [{ data: venture }, { data: shape }, { data: simulation }] = await Promise.all([
    supabase.from("ventures").select("id, name, target_user, geography").eq("id", id).maybeSingle(),
    supabase.from("venture_shapes").select("mvp_scope,problem_statement,differentiation").eq("venture_id", id).maybeSingle(),
    supabase.from("simulation_runs").select("technical_risk,build_progress_pct,product_quality_pct").eq("venture_id", id).order("created_at", { ascending: false }).limit(1).maybeSingle(),
  ]);
  if (!venture) notFound();

  const { data: pkg } = await supabase.from("build_packages").select("recommended_stack, backlog, cost_estimate, created_at").eq("venture_id", venture.id).order("created_at", { ascending: false }).limit(1).maybeSingle();
  const stack = pkg?.recommended_stack as unknown as RecommendedStack | undefined;
  const backlog = pkg?.backlog as unknown as BacklogItem[] | undefined;
  const cost = pkg?.cost_estimate as unknown as CostEstimate | undefined;
  const mustHave = (backlog ?? []).filter((i) => ["setup","core","auth","payments"].includes(i.category));
  const later = (backlog ?? []).filter((i) => ["polish","launch"].includes(i.category));
  const hasLegacyNumericPricing = Boolean(cost && typeof cost.totalMonthly === "number" && cost.items.every((item) => typeof item.monthlyCost === "number"));

  return (
    <main className="mx-auto max-w-6xl p-6">
      <VentureModeSection mode="simple">
        <p className="text-xs font-semibold uppercase tracking-[.2em] text-vs-primary">Step 5</p>
        <h1 className="mt-2 text-3xl font-semibold text-vs-fg">Plan what to build first</h1>
        <p className="mt-2 max-w-3xl text-sm leading-6 text-vs-fg-muted">Turn what you learned from research, shaping, monetization and simulation into a small, practical first version. You do not need to choose every technology yourself—the detailed technical view stays available in Pro mode.</p>
      </VentureModeSection>
      <VentureModeSection mode="pro"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-vs-primary">Build Studio</p><h1 className="mt-2 text-3xl font-semibold text-vs-fg">Turn the venture into an executable MVP</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-vs-fg-muted">Architecture and backlog suggestions are implementation aids. Provenance stays visible so reference-stack assumptions are not mistaken for researched technical truth.</p></div><Link href={`/venture/${venture.id}/technology`} className="text-sm font-medium text-vs-primary">Technology & ownership →</Link></div></VentureModeSection>

      {!pkg || !stack || !backlog || !cost ? <Card className="mt-6 max-w-3xl"><VentureModeSection mode="simple"><p className="mb-4 text-sm text-vs-fg-muted">Create a starting plan for the first version: what to build now, what can wait, and which vendor costs need current verification.</p></VentureModeSection><VentureModeSection mode="pro"><p className="mb-4 text-sm text-vs-fg-muted">Generate a starting stack recommendation, venture-specific task list, and a pricing-verification checklist. Build Studio does not fetch live vendor pricing.</p></VentureModeSection><GenerateButton ventureId={venture.id} /></Card> : <div className="mt-6 space-y-4">
        <VentureModeSection mode="simple" className="space-y-4">
          <Card className="border-vs-primary/20"><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">Your first version</p><h2 className="mt-2 text-xl font-semibold leading-8 text-vs-fg">{shape?.mvp_scope || "Use the must-have list below as the current first-version boundary."}</h2>{shape?.problem_statement && <p className="mt-3 text-sm leading-6 text-vs-fg-muted"><strong>The problem it should solve:</strong> {shape.problem_statement}</p>}</Card>
          <section className="grid gap-4 lg:grid-cols-[1.4fr_.8fr]"><Card><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">Build these first</p><p className="mt-1 text-sm text-vs-fg-muted">The smallest practical set before adding polish.</p></div><Badge status="primary">{mustHave.length} items</Badge></div><TaskList items={mustHave} /></Card><Card><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">Vendor pricing</p><p className="mt-2 text-2xl font-semibold text-vs-fg">{hasLegacyNumericPricing ? `$${cost.totalMonthly}/mo` : "Not connected"}</p><p className="mt-2 text-sm leading-6 text-vs-fg-muted">{hasLegacyNumericPricing ? "This is a stored estimate from an earlier build package, not live current pricing. Regenerate the package to remove unsourced vendor numbers." : "Build Studio does not fetch live vendor prices. Verify current official pricing for each required provider before budgeting."}</p>{simulation && <p className="mt-3 text-xs text-vs-fg-muted">The simulation currently models technical risk as <strong>{simulation.technical_risk}</strong>.</p>}</Card></section>
          <Card><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">Choose the kind of build path that fits you</p><div className="mt-3 grid gap-3 md:grid-cols-2">{stack.builderOptions.map((opt) => <div key={opt.name} className="rounded-vs-md border border-vs-border bg-vs-bg-subtle p-4"><p className="font-semibold text-vs-fg">{opt.name}</p><p className="mt-2 text-sm leading-6 text-vs-fg-muted">{opt.costNote}</p><div className="mt-2 text-xs text-vs-fg-muted">Speed: {opt.speed} · Control/ownership: {opt.ownership}</div></div>)}</div></Card>
          {later.length > 0 && <Card><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">Leave these for later</p><p className="mt-1 text-sm text-vs-fg-muted">Do not let polish delay the core test.</p><TaskList items={later} /></Card>}
          <Card className="border-vs-success/30"><p className="text-lg font-semibold text-vs-fg">You now have a build direction.</p><p className="mt-2 text-sm text-vs-fg-muted">After you launch or test with real people, come back and record what actually happened.</p><Link href={`/venture/${venture.id}/monitor`} className="mt-4 inline-flex rounded-vs-sm bg-vs-primary px-4 py-2 text-sm font-semibold text-vs-primary-fg">Continue to Step 6 · Learn →</Link></Card>
        </VentureModeSection>

        <VentureModeSection mode="pro" className="space-y-4">
          <section className="grid gap-4 lg:grid-cols-[1.3fr_1fr]"><Card><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">MVP direction</p><h2 className="mt-1 text-xl font-semibold text-vs-fg">{shape?.mvp_scope || "Use the generated backlog as the current MVP boundary."}</h2></div><Badge status="primary">IDEA-SPECIFIC HEURISTIC</Badge></div>{shape?.problem_statement && <p className="mt-3 text-sm leading-6 text-vs-fg-muted"><strong>Problem being built for:</strong> {shape.problem_statement}</p>}{shape?.differentiation && <p className="mt-2 text-sm leading-6 text-vs-fg-muted"><strong>Difference to preserve:</strong> {shape.differentiation}</p>}</Card><Card><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">Simulation handoff</p>{simulation ? <div className="mt-3 grid grid-cols-3 gap-2"><Mini label="Tech risk" value={simulation.technical_risk}/><Mini label="Build modeled" value={`${simulation.build_progress_pct}%`}/><Mini label="Quality modeled" value={`${simulation.product_quality_pct}%`}/></div> : <p className="mt-3 text-sm text-vs-fg-muted">No simulation has stress-tested the build assumptions yet.</p>}<p className="mt-3 text-xs text-vs-fg-muted">Simulation values are modeled state, not engineering estimates.</p></Card></section>
          <Card><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold uppercase tracking-wide text-vs-fg-muted">Reference architecture</p><p className="mt-1 text-xs text-vs-fg-muted">A starting implementation, not a personalized winner.</p></div><Badge status="neutral">REFERENCE ASSUMPTION</Badge></div><div className="mt-4 grid grid-cols-1 gap-3 sm:grid-cols-3"><StatTile label="Database" value={stack.database} /><StatTile label="Auth" value={stack.auth} /><StatTile label="Hosting" value={stack.hosting} /></div><div className="mt-4 grid gap-3 lg:grid-cols-2"><div className="rounded-vs-md border border-vs-border p-4"><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">API / service dependencies</p>{stack.notableApis.length ? <ul className="mt-2 list-inside list-disc text-sm text-vs-fg-muted">{stack.notableApis.map((api) => <li key={api}>{api}</li>)}</ul> : <p className="mt-2 text-sm text-vs-fg-muted">No notable external API was inferred by the build generator.</p>}</div><div className="rounded-vs-md border border-vs-border p-4"><p className="text-xs font-semibold uppercase tracking-wide text-vs-fg-muted">Ownership guardrails</p><ul className="mt-2 space-y-1 text-sm text-vs-fg-muted"><li>• Keep venture data exportable.</li><li>• Isolate replaceable third-party APIs behind owned interfaces.</li><li>• Avoid putting irreplaceable product logic only inside a vendor workflow.</li><li>• Re-evaluate lock-in after real usage establishes what deserves custom engineering.</li></ul></div></div></Card>
          <Card><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold uppercase tracking-wide text-vs-fg-muted">Build paths</p><p className="mt-1 text-xs text-vs-fg-muted">Different founder constraints can justify different implementation paths.</p></div><Badge status="primary">IDEA-SPECIFIC HEURISTIC</Badge></div><div className="mt-3 grid gap-3 md:grid-cols-2">{stack.builderOptions.map((opt) => <div key={opt.name} className="rounded-vs-md border border-vs-border bg-vs-bg-subtle p-4"><p className="text-sm font-medium text-vs-fg">{opt.name}</p><div className="mt-2 flex flex-wrap gap-1.5"><Badge status="primary">Speed: {opt.speed}</Badge><Badge status="neutral">Ownership: {opt.ownership}</Badge></div><p className="mt-2 text-xs leading-5 text-vs-fg-muted">{opt.costNote}</p></div>)}</div><p className="mt-3 text-xs text-vs-fg-muted">{stack.rationale}</p></Card>
          <section className="grid gap-4 lg:grid-cols-2"><Card><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold uppercase tracking-wide text-vs-fg-muted">Must-have first</p><Badge status="primary">{mustHave.length} tasks</Badge></div><TaskList items={mustHave} /></Card><Card><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold uppercase tracking-wide text-vs-fg-muted">Polish / launch after core</p><Badge status="neutral">{later.length} tasks</Badge></div><TaskList items={later} /></Card></section>
          <Card><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm font-semibold uppercase tracking-wide text-vs-fg-muted">Vendor pricing status</p><p className="mt-1 text-xs text-vs-fg-muted">Build Studio does not have a live pricing integration. Current provider prices must be verified at the source.</p></div><StatTile label={hasLegacyNumericPricing ? "Stored legacy estimate" : "Live pricing"} value={hasLegacyNumericPricing ? `$${cost.totalMonthly}/mo` : "Not connected"} /></div>{hasLegacyNumericPricing ? <><p className="mt-3 text-xs text-vs-fg-muted">Historical numeric values are shown only for backward compatibility and should not be treated as current pricing.</p><BarList className="mt-4" items={cost.items.filter((item): item is typeof item & { monthlyCost: number } => typeof item.monthlyCost === "number").map((item) => ({ label: item.name, sublabel: item.note, value: item.monthlyCost, valueLabel: `$${item.monthlyCost}` }))} /></> : <ul className="mt-4 space-y-2">{cost.items.map((item) => <li key={item.name} className="rounded-vs-md border border-vs-border p-3"><p className="text-sm font-medium text-vs-fg">{item.name}</p><p className="mt-1 text-xs leading-5 text-vs-fg-muted">{item.note}</p></li>)}</ul>}</Card>
        </VentureModeSection>
      </div>}
    </main>
  );
}

function TaskList({ items }: { items: BacklogItem[] }) { return items.length ? <ul className="mt-3 space-y-2">{items.map((item) => <li key={item.title} className="rounded-vs-md border border-vs-border p-3"><div className="flex items-baseline gap-2"><Badge status={CATEGORY_BADGE_STATUS[item.category] ?? "neutral"}>{CATEGORY_LABEL[item.category] ?? item.category}</Badge><span className="text-sm font-medium text-vs-fg">{item.title}</span></div><p className="mt-1 text-xs leading-5 text-vs-fg-muted">{item.description}</p></li>)}</ul> : <p className="mt-3 text-sm text-vs-fg-muted">No tasks in this group.</p>; }
function Mini({ label, value }: { label: string; value: string }) { return <div className="rounded-vs-sm bg-vs-bg-subtle p-3"><p className="text-[10px] uppercase tracking-wide text-vs-fg-muted">{label}</p><p className="mt-1 text-sm font-semibold capitalize text-vs-fg">{value}</p></div>; }
