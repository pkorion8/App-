import Link from "next/link"; import { Card, Badge } from "@venture-sandbox/ui";
const nodes=[["Saved context","Audience · geography · problem · promise · scope · differentiation"],["Research","App Store · World Bank · GitHub · explicit source gaps"],["Founder choices","Revenue model · selected monetization experiments · simulation decisions"],["Plans","Deterministic simulation · build package · operating cost"],["Learning","Manual real outcomes, kept separate from projections"]];
export default async function System({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="mx-auto max-w-4xl p-6"><Badge status="primary">SYSTEM VIEW</Badge><h1 className="mt-3 text-2xl font-semibold text-vs-fg">How this venture fits together</h1><p className="mt-2 text-sm text-vs-fg-muted">One context flows forward. Evidence, founder choices, simulated outcomes, and real outcomes keep distinct provenance.</p><div className="mt-6 space-y-3">{nodes.map(([a,b],i)=><Card key={a} className="relative"><span className="text-xs font-semibold text-vs-primary">0{i+1}</span><h2 className="mt-1 font-semibold text-vs-fg">{a}</h2><p className="mt-1 text-sm text-vs-fg-muted">{b}</p></Card>)}</div><Link href={`/venture/${id}/shape`} className="mt-6 inline-block text-sm font-semibold text-vs-primary">Review saved context →</Link></main>}
