/**
 * Worker entrypoint — reserved. Slice 1 (Foundation) ships no background
 * jobs; this stub exists so the workspace/CI shape matches the spec's
 * suggested repository (§21.3) ahead of the research/simulation queue work
 * that lands starting Slice 2.
 */
export {};
