# Architecture Decision Records

Reserved. No ADRs yet — Slice 1's stack/schema choices are documented
inline in README.md and AGENTS.md instead of as separate ADRs. Start
using this directory once a decision needs a durable, dated record
independent of the code that implements it.
