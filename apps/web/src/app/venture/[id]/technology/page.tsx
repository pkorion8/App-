import Link from "next/link"; import { Card, Badge } from "@venture-sandbox/ui";
const options: [string,string,string,string][]=[["Fastest MVP","Managed web app","REFERENCE ASSUMPTION","Fast setup; provider dependence remains."],["Lowest recurring cost","Free tiers first","IDEA-SPECIFIC HEURISTIC","An operating-cost floor, not a scale forecast."],["Best ownership","Portable database + export path","HEURISTIC RECOMMENDATION","Ownership improves when data and interfaces stay portable."],["Balanced option","Next.js + Supabase + Vercel","REFERENCE ASSUMPTION","The project reference stack—not a researched winner."]];
export default async function Technology({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="mx-auto max-w-4xl p-6"><p className="text-sm font-semibold text-vs-primary">CAN IT BE BUILT?</p><h1 className="mt-2 text-2xl font-semibold text-vs-fg">Technology &amp; ownership</h1><p className="mt-2 text-sm text-vs-fg-muted">Separate source evidence from recommendations and fixed product assumptions.</p><div className="mt-5 grid gap-4 sm:grid-cols-2">{options.map(([title,value,label,note])=><Card key={title}><Badge status={label.includes("REFERENCE")?"neutral":"primary"}>{label}</Badge><h2 className="mt-3 font-semibold text-vs-fg">{title}</h2><p className="mt-1 text-lg text-vs-fg">{value}</p><p className="mt-2 text-sm text-vs-fg-muted">{note}</p></Card>)}</div><Card className="mt-4"><Badge status="warning">PARTIAL</Badge><p className="mt-3 text-sm text-vs-fg-muted">GitHub activity is researched when available; it indicates technical territory, not that a particular stack is best. See source-level records in the Evidence Explorer.</p><div className="mt-3 flex gap-4"><Link href={`/venture/${id}/evidence`} className="text-sm text-vs-primary">Open evidence →</Link><Link href={`/venture/${id}/build`} className="text-sm text-vs-primary">Open Build Studio →</Link></div></Card></main>}
